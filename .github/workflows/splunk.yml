name: ci

on:
  push:
    branches:
      - "main"

jobs:
  build:
    runs-on: ubuntu-latest
    container: splunk/splunk:latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v3